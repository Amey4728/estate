/** @type {import('tailwindcss').Config} */
export default {
  // the site component lives at the repo root, so scan it explicitly
  content: ["./index.html", "./src/**/*.{js,jsx}", "./mitesh-realestate.jsx"],
  theme: { extend: {} },
  plugins: [],
};
